<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:SmartExpenseTracker.Views"
       x:Class="SmartExpenseTracker.AppShell"
       Shell.NavBarIsVisible="True">

    <TabBar>
        <ShellContent Title="Dashboard" Icon="dashboard.png"
                      ContentTemplate="{DataTemplate views:DashboardPage}"
                      Route="DashboardPage" />
        <ShellContent Title="Expenses" Icon="expenses.png"
                      ContentTemplate="{DataTemplate views:ExpenseListPage}"
                      Route="ExpenseListPage" />
        <ShellContent Title="Budget" Icon="budget.png"
                      ContentTemplate="{DataTemplate views:BudgetPage}"
                      Route="BudgetPage" />
        <ShellContent Title="Goals" Icon="goals.png"
                      ContentTemplate="{DataTemplate views:GoalsPage}"
                      Route="GoalsPage" />
        <ShellContent Title="Settings" Icon="settings.png"
                      ContentTemplate="{DataTemplate views:SettingsPage}"
                      Route="SettingsPage" />
    </TabBar>
</Shell>

